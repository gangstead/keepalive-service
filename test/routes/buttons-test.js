'use strict';

const expect = require('chai').expect;

describe('Buttons route', () => {
  describe('GET /buttons', () => {
    it('should return buttons', () => {
      expect(true);
    });
  });
});
